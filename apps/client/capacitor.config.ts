import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.solangebernard.mobile',
  appName: 'SolangeBernard Mobile',
  webDir: 'dist/mobile/browser',
};

export default config;
